import 'package:flutter/material.dart';
import 'package:sys_gallery/src/constants/app_colors_constants.dart';

//Icons

const kAddIcon = Icon(Icons.add, color: kWhite);

const kRefreshIcon = Icon(Icons.refresh, color: kWhite);

const kArrowBackIcon = Icon(Icons.arrow_back, color: kWhite);

const kSearchIcon = Icon(Icons.search, color: kCharcoalPrimary);

const kAddDarkIcon = Icon(Icons.add, color: kDark, size: 44);

const kCloseIcon = Icon(Icons.close, color: kSilver);

const kCheckCircleIcon = Icon(Icons.check_circle, color: Color(0xFF37C46B), size: 45);

const kErrorIcon = Icon(Icons.highlight_off, color: Color(0xFFF55124), size: 45);
